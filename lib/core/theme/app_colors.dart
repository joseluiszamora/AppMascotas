import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primarios
  static const Color primary = Color(0xFF7B9EFF);
  static const Color primaryLight = Color(0xFFB8CEFF);
  static const Color primaryDark = Color(0xFF4D6FD1);

  // Secundarios (pastel)
  static const Color secondary = Color(0xFFFFB3C6);
  static const Color secondaryLight = Color(0xFFFFD6E3);

  // Neutros
  static const Color background = Color(0xFFF8F9FE);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF0F2FB);
  static const Color border = Color(0xFFE4E8F5);

  // Texto
  static const Color textPrimary = Color(0xFF1A1D2E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textHint = Color(0xFFB0B7C3);

  // Semánticos
  static const Color success = Color(0xFF6FCF97);
  static const Color warning = Color(0xFFFBBF24);
  static const Color error = Color(0xFFF87171);
  static const Color info = Color(0xFF60A5FA);
}
